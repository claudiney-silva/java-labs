server:
  port: 8888

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config
          #search-locations: file://${user:home}/config
  security:
    user:
      name: admin
      password: password
